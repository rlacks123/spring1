<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yedam.app.dept.mapper.DeptMapper">

    <!-- 전체 조회 -->
    <select id="selectDeptList" resultType="com.yedam.app.dept.service.DeptVO">
        SELECT department_id
            , department_name
            , manager_id
            , location_id
        FROM departments
        ORDER BY department_id
    </select>

    <!-- 단건 조회 -->
    <select id="selectDeptInfo" parameterType="com.yedam.app.dept.service.DeptVO" 
        resultType="com.yedam.app.dept.service.DeptVO">
        SELECT department_id
            , department_name
            , manager_id
            , location_id
        FROM departments
        WHERE department_id = #{departmentId}
    </select>

    <!-- 등록 -->
    <insert id="insertDeptInfo" parameterType="com.yedam.app.dept.service.DeptVO">
        <selectKey keyProperty="departmentId" resultType="Integer" order="BEFORE">
            SELECT NVL(MAX(department_id), 100) + 1 FROM departments
        </selectKey>
        INSERT INTO departments
            (
                department_id
                , department_name
                , manager_id
                , location_id
            )
        VALUES
            (
                #{departmentId}
                , #{departmentName}
                , #{managerId}
                , #{locationId}
            )
    </insert>

    <!-- 수정 -->
    <update id="updateDeptInfo" parameterType="com.yedam.app.dept.service.DeptVO">
        UPDATE departments
        SET
            department_name = #{departmentName}
            , manager_id = #{managerId}
            , location_id = #{locationId}
        WHERE department_id = #{departmentId}
    </update>

    <!-- 삭제 -->
    <delete id="deleteDeptInfo" parameterType="com.yedam.app.dept.service.DeptVO">
        DELETE FROM departments
        WHERE department_id = #{departmentId}
    </delete>

</mapper>
